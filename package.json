{
  "name": "herdr-agent-nav",
  "version": "0.1.0",
  "private": true,
  "description": "Keyboard-first agent navigation and naming for Herdr",
  "type": "module",
  "scripts": { "test": "node --test" },
  "engines": { "node": ">=18" }
}
